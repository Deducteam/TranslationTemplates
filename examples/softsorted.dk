tm : Type.
Set : Type.
Prop : Type.
def Prf : Prop -> Type.
oftype : tm -> Set -> Type.

imp : Prop -> Prop -> Prop.
[p,q] Prf (imp p q) --> Prf p -> Prf q.

all : a : Set -> (x : tm -> oftype x a -> Prop) -> Prop.
[a,p] Prf (all a p) --> x : tm -> h : oftype x a -> Prf (p x h).
